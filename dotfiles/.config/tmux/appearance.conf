# Tmux appearance configuration
# ==============================

# Default status bar style (commented out - using plugin instead)
# set -g status-position top
# set -g status-justify absolute-centre
# set -g status-style "bg=default"
# set -g window-status-current-style "fg=blue bold"

# minimal-tmux-status plugin configuration
set -g @minimal-tmux-status "top"
set -g @minimal-tmux-justify "centre"
set -g @minimal-tmux-indicator false

# Enables or disables the left and right status bar
set -g @minimal-tmux-right false
set -g @minimal-tmux-left false

# Expanded icon (fullscreen icon)
set -g @minimal-tmux-expanded-icon "󰊓 "

# Show expanded icons on all tabs (default is false)
# false will make it visible for the current tab only
set -g @minimal-tmux-show-expanded-icons-for-all-tabs true

# Extra text in status bar
set -g @minimal-tmux-status-right-extra ""
set -g @minimal-tmux-status-left-extra ""

# Selection box style - rounded () or edged <>
# Default is nothing, when set to true default is edged
set -g @minimal-tmux-use-arrow true
set -g @minimal-tmux-right-arrow ""
set -g @minimal-tmux-left-arrow ""
